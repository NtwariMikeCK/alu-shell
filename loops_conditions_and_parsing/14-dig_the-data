#!/usr/bin/env bash
# Define the function to process the log file

# Parse the Apache log file and group by IP and HTTP status code, then count occurrences
awk '{print $1, $9}' access.log | sort | uniq -c | sort -nr

